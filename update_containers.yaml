---
  - hosts: mediaserver
    vars_files:
      - 'vars/vault.yaml'
    
    tasks:
      - name: Pull to update and restart
        community.docker.docker_compose_v2:
          project_src: /config
          pull: always
          state: present
        register: output